export enum Command {
  v = '-v',
  version = '--version',
  h = '-h',
  help = '--help',
  create = 'create',
  serve = 'serve',
  build = 'build'
}

export enum Scope {
  app = 'app',
  application = 'application',
  c = 'c',
  component = 'component',
  i = 'i',
  injectable = 'injectable'
}
